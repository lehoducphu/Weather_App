<!-- res/drawable/background_translucent_border_dark.xml -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Background with rounded corners -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#33000000"/> <!-- Màu đen trong suốt (20%) -->
            <corners android:radius="16dp"/> <!-- Độ cong của các góc -->
        </shape>
    </item>
    <!-- Border with rounded corners -->
    <item android:left="2dp" android:right="2dp" android:top="2dp" android:bottom="2dp">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent"/>
            <corners android:radius="14dp"/> <!-- Độ cong của các góc border -->
        </shape>
    </item>
</layer-list>